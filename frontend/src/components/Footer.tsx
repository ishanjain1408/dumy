import '../styles/Footer.css'

const Footer = () => {
    return (
        <footer>
            <p>&copy; {new Date().getFullYear()} Ishan's Notes App</p>
        </footer>
    );
};

export default Footer;